spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbops_meta
    username: root
    password: metapass123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true 

dbops:
  encrypt-key: "dbops-secret-key-32-characters!!"   # AES-256용 32자 키
  health-check:
    interval-ms: 30000   # 헬스체크 주기 30초
    timeout-ms: 3000     # DB 접속 타임아웃 3초